#pragma once

class Vector {
public:
	float x, y, z;
	Vector(float x, float y, float z) : x(x), y(y), z(z) {};
};

class Angle {
public:
	float p, y, r;
	Angle(float p, float y, float r) : p(p), y(y), r(r) {};
};